pipeline {
    agent { label 'lnx' }
    stages {
        stage('Build') {
            steps {
                sh 'fake.sh "compile" 6'
                sh 'fake.sh "linking" 3'
            }
        }
        stage('Test') {
            steps {
                sh 'fake.sh "Testing product" 10'
            }
        }
        stage('Deploy') {
            steps {
                sh 'fake.sh "Deploy" 3'
            }
        }
    }
}
