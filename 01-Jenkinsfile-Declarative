pipeline {
    agent { label 'lnx' }
    stages {
        stage('Build') {
            steps {
                scm checkout
                sh 'fake.sh "compile" 6'
                sh 'fake.sh "linking" 3'
            }
        }
        stage('Test') {
            steps {
                scm checkout
                sh 'fake.sh "Testing product" 10'
            }
        }
        stage('Deploy') {
            steps {
                scm checkout
                sh 'fake.sh "Deploy" 3'
            }
        }
    }
}
